# Logpilot 会自动加载「当前工作目录」下的 .env
# 将本文件复制为 .env 后按需填写即可：
#   cp .env.example .env
#
# ================== 最小可用（推荐先跑起来） ==================
# 默认使用 OpenAI 兼容 API（AI_PROVIDER 默认 openai）
# 只需要二选一填写 Key：
# 1) 仅配置 OPEN_AI_API_KEY        → 默认使用 openai
# 2) 仅配置 DEEPSEEK_API_KEY       → 默认使用 deepseek
# 3) 两者都配置或都不配置         → 需显式设置 AI_PROVIDER，否则会提示错误
OPEN_AI_API_KEY=sk-your-openai-key
DEEPSEEK_API_KEY=sk-your-deepseek-key

# ================== 提供方（可选） ==================
# openai   - OpenAI 或任意 OpenAI 兼容网关（默认）
# deepseek - DeepSeek 官方 API（同样走 OpenAI 兼容的 /v1/chat/completions）
# AI_PROVIDER=openai

# ================== API Base（可选） ==================
# 统一使用 OPEN_AI_BASE 作为 OpenAI 兼容网关 Base URL：
#   - openai 默认：   https://api.openai.com/v1
#   - deepseek 默认： https://api.deepseek.com/v1
# 若你使用自建网关或代理，可通过 OPEN_AI_BASE 覆盖：
#
# OPEN_AI_BASE=https://api.openai.com/v1
#
# ================== 模型（可选） ==================
# openai 默认：   gpt-4o-mini
# deepseek 默认： deepseek-chat
# 可按需覆盖：
# OPEN_AI_MODEL=gpt-4o-mini
# DEEPSEEK_MODEL=deepseek-chat
